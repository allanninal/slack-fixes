{
  "name": "slack-fixes",
  "version": "1.0.0",
  "description": "Read-only Python and Node.js scripts that find Slack app problems through the API \u2014 ok:false behind an HTTP 200, missing scopes, a bot outside the channel it posts to, and pagination nobody followed. They report and print the repair; they never write",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT"
}
